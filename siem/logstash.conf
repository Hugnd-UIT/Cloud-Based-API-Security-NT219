input {
  beats { 
    port => 5044
    ssl => true 
    ssl_certificate => "/usr/share/logstash/config/certs/db_client.crt"
    ssl_key => "/usr/share/logstash/config/keys/db_client.key"
    ssl_certificate_authorities => ["/usr/share/logstash/config/certs/ca.crt"]
    ssl_verify_mode => "none" # Đổi từ force_peer sang none để debug mTLS trước
  }
}

output {
  elasticsearch {
    hosts => ["https://elasticsearch:9200"] # ĐẢM BẢO LÀ HTTPS
    ssl_certificate_verification => false # Quan trọng để không bị lỗi "failed to respond"
    user => "elastic"
    password => "Payshield@123"
    index => "siem-logs-%{+YYYY.MM.dd}"
  }
}